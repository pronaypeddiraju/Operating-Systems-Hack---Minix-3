#include <iostream>
#include <stdlib.h>

void 00808()
{
    struct node *temp;
    temp=hole_head;
    while(temp!=NULL)
    {
    cout<<"\n Base address : ";;
    cout<<temp->h_base;
    cout<<"\n Size of hole : ";
    cout<<tmep->h_len;
    cout<<"\n";
    temp=temp->next;
    }
}
